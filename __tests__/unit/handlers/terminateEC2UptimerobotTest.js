// Import all functions from restartTimeandTideFunction
const lambda = require('../../../src/handlers/terminateEC2UptimerobotHandler.js');

// This includes all tests for restartTimeandTideHadler()
describe('Test for terminate-ec2-uptimerobot', function () {
    // This test invokes helloFromLambdaHandler() and compare the result 
    /*    
    it('Verifies successful response', async () => {
        // Invoke helloFromLambdaHandler()
        const result = await lambda.terminateEC2UptimerobotHandler();
        
        const expectedResult = 'Hello from Lambda!';
        // Compare the result with the expected result
        expect(result).toEqual(expectedResult);
    
    });
    */
});
